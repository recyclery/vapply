<%= form_for(@caseworker) do |f| %>
  <% if @caseworker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@caseworker.errors.count, "error") %> prohibited this caseworker from being saved:</h2>

      <ul>
      <% @caseworker.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table>
    <tr>
      <td><%= f.label :name %></td>
      <td><%= f.text_field :name %></td>
    </tr><tr>
    
      <td><%= f.label :organization %></td>
      <td><%= f.text_field :organization %></td>
    </tr><tr>
      <td><%= f.label :address1 %></td>
      <td><%= f.text_field :address1 %></td>
    </tr><tr>
      <td><%= f.label :address2 %></td>
      <td><%= f.text_field :address2 %></td>
    </tr><tr>
      <td><%= f.label :phone %></td>
      <td><%= f.text_field :phone %></td>
    </tr>
    <tr>
      <td><%= f.label :email %></td>
      <td><%= f.text_field :email %></td>
    </tr>
<% if current_user.is_admin? -%>
    <tr>
      <td><%= f.label :limit %></td>
      <td><%= f.text_field :limit %></td>
    </tr>
<% end -%>
  </table>
<br />
  <div class="actions"><%= f.submit "Update Info" %></div>
<% end %>
